{
  "name": "sao-jose-landing",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host :: --port 8080",
    "build": "vite build",
    "preview": "vite preview --host :: --port 8080"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
